{
  "name": "formpilot-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0"
  }
}
